[
  {
    "name": "context_brief",
    "description": "One-call session-start digest — call this FIRST in every new conversation instead of separate get_project/retrieve/task_list calls. Returns a token-budgeted briefing: identity (L4 user preferences), last_session (recent L2 summaries), project_knowledge (pinned checkpoints + top L3 facts), recent_changes (items stored in the last N days), recent_attempts (tried-and-failed records so you don't repeat dead ends), open_tasks, and stats (item counts by layer).",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "session_id": {
          "type": "string",
          "description": "Project slug — when no explicit project is passed, this scopes the WHOLE brief (collection routing, memories, and open_tasks) to that project"
        },
        "project": {
          "type": "string",
          "description": "Project slug — scopes the whole brief to this project only (memories, recent changes, attempts, open_tasks). Falls back to session_id, then the active project (optional)"
        },
        "max_tokens": {
          "type": "integer",
          "description": "Token budget for the whole briefing (default: 2000)"
        },
        "recent_days": {
          "type": "integer",
          "description": "Window for the recent_changes section in days (default: 7)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_pack",
    "description": "Assemble ONE prompt-ready context block for a specific task — query-driven, unlike context_brief's session-start digest. Returns structured sections (identity, relevant memories with 1-hop relations, pinned checkpoints, dead-end attempts, open tasks, matching skills, code-index hits) AND a 'rendered' markdown string ready to inject into a prompt, with provenance and staleness markers. Unspent section budget is reallocated to fuller sections. Call this before starting any significant task — it replaces separate retrieve + task_list + skill_find calls.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "The task or question to pack context for"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project slug — overrides the active-project pointer for this call (optional)"
        },
        "session_id": {
          "type": "string",
          "description": "Session identifier for session scoping (optional)"
        },
        "max_tokens": {
          "type": "integer",
          "description": "Token budget for the whole pack (default: 3000)"
        },
        "include_code": {
          "type": "boolean",
          "description": "Include code/doc index hits when the project has an index (default: true)"
        }
      },
      "required": [
        "query",
        "user_id"
      ]
    }
  },
  {
    "name": "context_retrieve",
    "description": "Retrieve relevant context items for the current query from all memory layers. Each item carries a `citation` object (sourceId, url, title, offsets) so an answer can attribute it.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Current question or topic"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "session_id": {
          "type": "string",
          "description": "Project namespace or session identifier. Pass the project slug (e.g. 'taty-v2') to scope retrieval to that project only and avoid cross-project contamination. Omit only for cross-project or global queries."
        },
        "project": {
          "type": "string",
          "description": "Project name to filter results by (e.g. 'taty-v2'). Filters at the Qdrant level for efficient project-scoped retrieval. Can be used alongside or instead of session_id."
        },
        "top_k": {
          "type": "integer",
          "description": "Max items to return (default: 10)"
        },
        "memory_layers": {
          "type": "array",
          "items": {
            "type": "string",
            "enum": [
              "L1",
              "L2",
              "L3",
              "L4"
            ]
          },
          "description": "Which memory layers to query (default: all)"
        },
        "expand_relations": {
          "type": "boolean",
          "description": "Also include memories linked to the retrieved items via relations (1-hop, marked with via_relation). Default: false"
        },
        "filters": {
          "type": "object",
          "description": "Metadata equality filters pushed down to the vector store's payload filter, e.g. {\"locale\": \"en\", \"source_id\": \"help-42\"}. Applied before top_k, not after, so a filtered search still returns up to top_k items rather than whatever survives trimming."
        }
      },
      "required": [
        "query",
        "user_id"
      ]
    }
  },
  {
    "name": "context_store",
    "description": "Store content into the appropriate memory layer. Pass either a single 'content' + 'memory_layer', or 'items' to store several facts in one call (preferred when saving multiple facts — one round-trip instead of N). After storing (L2-L4), same-purpose detection runs automatically: near-identical memories are auto-linked with alternative_to edges (auto_linked in the response); probable matches are returned as suggestions to confirm with context_relate (suggestions in the response). To record a failed approach (so future sessions don't repeat it), store an L2 item with metadata {type: 'attempt', goal, approach, outcome: 'failed', why} — context_brief surfaces these in its recent_attempts section.",
    "parameters": {
      "type": "object",
      "properties": {
        "content": {
          "type": "string",
          "description": "Content to store (single-item mode)"
        },
        "items": {
          "type": "array",
          "description": "Batch mode: list of items to store in one call. Each item: {content, memory_layer, importance?, metadata?, confidence?, session_id?, origin?}. When provided, top-level content/memory_layer are ignored.",
          "items": {
            "type": "object",
            "properties": {
              "content": {
                "type": "string"
              },
              "memory_layer": {
                "type": "string",
                "enum": [
                  "L1",
                  "L2",
                  "L3",
                  "L4"
                ]
              },
              "importance": {
                "type": "number"
              },
              "metadata": {
                "type": "object"
              },
              "confidence": {
                "type": "number"
              },
              "session_id": {
                "type": "string"
              },
              "origin": {
                "type": "string",
                "enum": [
                  "user-stated",
                  "agent-inferred",
                  "ingested-from-file",
                  "ingested-from-web",
                  "web-search"
                ]
              }
            },
            "required": [
              "content",
              "memory_layer"
            ]
          }
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "importance": {
          "type": "number",
          "description": "Importance score 0.0-1.0"
        },
        "memory_layer": {
          "type": "string",
          "enum": [
            "L1",
            "L2",
            "L3",
            "L4"
          ]
        },
        "session_id": {
          "type": "string",
          "description": "Project namespace or session identifier. Use the project slug (e.g. 'taty-v2') for project-specific facts so they are retrievable in isolation. Use a descriptive slug (e.g. 'user-preferences') for global or cross-project facts."
        },
        "metadata": {
          "type": "object",
          "description": "Extra metadata (optional). Conventions: files: [paths] — the files this fact refers to; their content hashes are stored so retrieval can flag the memory possibly_stale when they change. fact_type: 'file-location' | 'config' | 'architecture' | 'preference' — controls type-aware TTL decay in GC (file locations expire fast, preferences slowly). type: 'attempt' marks a tried-and-failed record (see description)."
        },
        "confidence": {
          "type": "number",
          "description": "Confidence score 0.0-1.0 (default: 1.0)"
        },
        "origin": {
          "type": "string",
          "enum": [
            "user-stated",
            "agent-inferred",
            "ingested-from-file",
            "ingested-from-web",
            "web-search"
          ],
          "description": "Provenance of this fact: 'user-stated' when the user said it directly, 'agent-inferred' for your own conclusions (default), 'web-search' for facts found online. Ingested content is tagged automatically. Stored in metadata.origin and returned on retrieval so trust level is always visible."
        },
        "project": {
          "type": "string",
          "description": "Project slug to operate in — overrides the active-project pointer for this call. Pass it in multi-session setups so concurrent sessions in different projects don't route into each other's collections (optional)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_summarize",
    "description": "Summarize the working memory for a session. Runs async and off the critical path by default; pass sync: true to get the summary back in the same call.",
    "parameters": {
      "type": "object",
      "properties": {
        "session_id": {
          "type": "string",
          "description": "Session to summarize"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "max_tokens": {
          "type": "integer",
          "description": "Max summary length in tokens (default: 500)"
        },
        "focus": {
          "type": "string",
          "description": "What to focus on in the summary (optional)"
        },
        "sync": {
          "type": "boolean",
          "description": "Wait for the summary and return it in this call as {summary, keyEntities, tokensSaved}. Default false, which schedules the work and returns immediately — right for background compaction, useless when the summary is needed now."
        }
      },
      "required": [
        "session_id",
        "user_id"
      ]
    }
  },
  {
    "name": "context_score",
    "description": "Score a list of context items by relevance to a query.",
    "parameters": {
      "type": "object",
      "properties": {
        "items": {
          "type": "array",
          "description": "List of context items to score",
          "items": {
            "type": "object"
          }
        },
        "query": {
          "type": "string",
          "description": "Query to score against"
        }
      },
      "required": [
        "items",
        "query"
      ]
    }
  },
  {
    "name": "context_checkpoint",
    "description": "Save a named checkpoint — a pinned, high-importance L3 memory snapshot of a decision, milestone, or code state. Use this when something significant happens that should be permanently remembered and retrievable by name.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Checkpoint name (e.g. 'v1-auth-refactor', 'before-db-migration')"
        },
        "content": {
          "type": "string",
          "description": "What to snapshot — decision rationale, code summary, milestone description"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project name for filtering (optional)"
        },
        "session_id": {
          "type": "string",
          "description": "Project namespace or session identifier. Use the project slug (e.g. 'taty-v2') to associate this checkpoint with a specific project."
        }
      },
      "required": [
        "name",
        "content",
        "user_id"
      ]
    }
  },
  {
    "name": "context_deprecate",
    "description": "Mark an existing memory item as deprecated. The item is NOT deleted — it stays in the store but is excluded from normal retrieval. Use this when a checkpoint or fact is superseded by a newer one. Pass superseded_by to record which item replaces it (creates a 'supersedes' relation automatically).",
    "parameters": {
      "type": "object",
      "properties": {
        "item_id": {
          "type": "string",
          "description": "ID of the item to deprecate"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "reason": {
          "type": "string",
          "description": "Why this item is being deprecated (optional)"
        },
        "superseded_by": {
          "type": "string",
          "description": "ID of the newer item that replaces this one — records a 'supersedes' relation (new item → deprecated item) so replacement history is traceable (optional)"
        },
        "project": {
          "type": "string",
          "description": "Project slug to operate in — overrides the active-project pointer for this call. Pass it in multi-session setups so concurrent sessions in different projects don't route into each other's collections (optional)"
        }
      },
      "required": [
        "item_id",
        "user_id"
      ]
    }
  },
  {
    "name": "context_relate",
    "description": "Link two memory items with a typed relation (source → target) so they can be retrieved and handled together. Known types: related_to, supersedes, part_of, depends_on, caused_by — custom types are also accepted.",
    "parameters": {
      "type": "object",
      "properties": {
        "source_id": {
          "type": "string",
          "description": "ID of the source memory item"
        },
        "target_id": {
          "type": "string",
          "description": "ID of the target memory item"
        },
        "relation_type": {
          "type": "string",
          "description": "Relation type (default: related_to). Known: related_to, supersedes, part_of, depends_on, caused_by. Custom strings allowed."
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project name tag (optional)"
        },
        "metadata": {
          "type": "object",
          "description": "Extra metadata for the edge (optional)"
        }
      },
      "required": [
        "source_id",
        "target_id",
        "user_id"
      ]
    }
  },
  {
    "name": "context_unrelate",
    "description": "Remove a relation between two memory items — by relation_id, or by the source_id + target_id pair (optionally narrowed by relation_type).",
    "parameters": {
      "type": "object",
      "properties": {
        "relation_id": {
          "type": "string",
          "description": "ID of the relation to remove"
        },
        "source_id": {
          "type": "string",
          "description": "Source item ID (used with target_id when relation_id is not given)"
        },
        "target_id": {
          "type": "string",
          "description": "Target item ID (used with source_id when relation_id is not given)"
        },
        "relation_type": {
          "type": "string",
          "description": "Only remove edges of this type (optional, with source_id/target_id)"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project slug to operate in — overrides the active-project pointer for this call. Pass it in multi-session setups so concurrent sessions in different projects don't route into each other's collections (optional)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_related",
    "description": "List the memories linked to an item via relations, with the connecting edges. Follows supersedes chains into deprecated items (direct lookup, not search).",
    "parameters": {
      "type": "object",
      "properties": {
        "item_id": {
          "type": "string",
          "description": "Memory item ID whose neighbors to fetch"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "relation_type": {
          "type": "string",
          "description": "Only follow edges of this type (optional)"
        },
        "direction": {
          "type": "string",
          "enum": [
            "out",
            "in",
            "both"
          ],
          "description": "Edge direction relative to item_id (default: both)"
        },
        "project": {
          "type": "string",
          "description": "Project slug to operate in — overrides the active-project pointer for this call. Pass it in multi-session setups so concurrent sessions in different projects don't route into each other's collections (optional)"
        }
      },
      "required": [
        "item_id",
        "user_id"
      ]
    }
  },
  {
    "name": "context_get",
    "description": "Fetch a single memory item directly by its ID — no vector search. Works for deprecated items too. Use when you already know the item id (e.g. from a previous store/retrieve/related call).",
    "parameters": {
      "type": "object",
      "properties": {
        "item_id": {
          "type": "string",
          "description": "Memory item ID"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project slug to operate in — overrides the active-project pointer for this call. Pass it in multi-session setups so concurrent sessions in different projects don't route into each other's collections (optional)"
        }
      },
      "required": [
        "item_id",
        "user_id"
      ]
    }
  },
  {
    "name": "context_visualize",
    "description": "Render the memory graph (items + relation edges) as a Mermaid flowchart. Nodes are colored by memory layer (L1-L4), deprecated items are dashed, pinned items get a thick border; edges are labeled with their relation type. The returned mermaid string renders natively in Claude and any Mermaid viewer. Best for up to ~50 nodes.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Filter items by project tag (optional)"
        },
        "memory_layer": {
          "type": "string",
          "enum": [
            "L1",
            "L2",
            "L3",
            "L4"
          ],
          "description": "Only include items from this layer (optional; L4 reads the shared user collection)"
        },
        "relations_only": {
          "type": "boolean",
          "description": "Only show items that have at least one relation edge (default: false)"
        },
        "include_deprecated": {
          "type": "boolean",
          "description": "Include deprecated items, e.g. supersedes targets (default: true)"
        },
        "direction": {
          "type": "string",
          "enum": [
            "LR",
            "TD"
          ],
          "description": "Graph layout direction: left-to-right or top-down (default: LR)"
        },
        "limit": {
          "type": "integer",
          "description": "Max items to include (default: 50)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_alternatives",
    "description": "Answer 'what can I use for X?' — semantic search for a purpose (e.g. 'approve button'), returning each matching memory grouped with its alternatives (items linked via alternative_to or used_for edges). Alternatives are detected automatically at store time: near-identical purposes are auto-linked, probable matches are returned as suggested_relations in the store response.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "query": {
          "type": "string",
          "description": "The purpose to search for, e.g. 'approve button component'"
        },
        "top_k": {
          "type": "integer",
          "description": "Max groups to return (default: 5)"
        },
        "project": {
          "type": "string",
          "description": "Project slug to operate in — overrides the active-project pointer for this call. Pass it in multi-session setups so concurrent sessions in different projects don't route into each other's collections (optional)"
        }
      },
      "required": [
        "user_id",
        "query"
      ]
    }
  },
  {
    "name": "context_ingest",
    "description": "Parse and ingest a file, a URL, or caller-held text into memory as chunks. Supports .docx, .pdf, .md, source code files (.py, .js, .ts, .go, .rs, ...), any http(s):// URL, or raw text passed in `text` when the caller already holds the content. Pass `source_id` to make the document addressable later: re-ingesting or retiring it with context_deprecate_source works on that id. Each chunk is embedded and stored automatically.",
    "parameters": {
      "type": "object",
      "properties": {
        "source": {
          "type": "string",
          "description": "Absolute file path or URL to ingest. Provide exactly one of source or text."
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "memory_layer": {
          "type": "string",
          "enum": [
            "L1",
            "L2",
            "L3",
            "L4"
          ],
          "description": "Memory layer to store chunks in (default: L3)"
        },
        "importance": {
          "type": "number",
          "description": "Importance score 0.0-1.0 (default: 0.8)"
        },
        "project": {
          "type": "string",
          "description": "Project name tag for filtering (optional)"
        },
        "session_id": {
          "type": "string",
          "description": "Project namespace. Always set this to the project name (e.g. 'taty-v2') so ingested chunks are scoped to that project and retrievable in isolation without cross-project contamination."
        },
        "text": {
          "type": "string",
          "description": "Content the caller already holds, ingested directly instead of being fetched. Provide exactly one of source or text."
        },
        "source_id": {
          "type": "string",
          "description": "The caller's own identifier for this document, stored on every chunk. A source id is the document's identity — there is no separate document entity — so it is what context_deprecate_source retires and what a re-sync replaces (optional)."
        },
        "url": {
          "type": "string",
          "description": "Citation URL recorded on every chunk, for text that was fetched elsewhere (optional)."
        },
        "title": {
          "type": "string",
          "description": "Document title recorded on every chunk, used when citing it (optional)."
        },
        "locale": {
          "type": "string",
          "description": "Document locale (e.g. 'en'), recorded on every chunk and filterable at retrieval (optional)."
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_index",
    "description": "Index a file, directory, or glob into the project's persistent code/doc index (a separate ctx_<slug>__index collection — never mixed into memories). Idempotent: unchanged files are skipped via content hashes, changed files re-embed only their changed chunks, and stale chunks are swept. Respects .gitignore in git repos. Requires an active or explicit project.",
    "parameters": {
      "type": "object",
      "properties": {
        "source": {
          "type": "string",
          "description": "Absolute path to a file or directory, or a glob pattern"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project slug (optional — defaults to the active project; required if none is set)"
        },
        "force": {
          "type": "boolean",
          "description": "Re-embed files even if their hash is unchanged (default: false)"
        },
        "max_files": {
          "type": "integer",
          "description": "Cap on files per call (default: 500)"
        }
      },
      "required": [
        "source",
        "user_id"
      ]
    }
  },
  {
    "name": "context_index_search",
    "description": "Hybrid search over the project's persistent code/doc index: dense semantic search fused with exact-symbol and full-text keyword matching, so exact identifiers (function/class names) are found even when semantic search misses them. Returns snippets with path, symbol, and line numbers, flagged possibly_stale when the file changed on disk since indexing. Backtick an identifier in the query for an exact-match boost.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Natural-language question or an identifier (backtick exact symbols for a boost)"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project slug (optional)"
        },
        "top_k": {
          "type": "integer",
          "description": "Max hits (default: 5)"
        },
        "language": {
          "type": "string",
          "description": "Filter by language, e.g. 'py', 'ts' (optional)"
        },
        "path_prefix": {
          "type": "string",
          "description": "Only hits whose path starts with this prefix (optional)"
        }
      },
      "required": [
        "query",
        "user_id"
      ]
    }
  },
  {
    "name": "context_index_status",
    "description": "Report what's in the project's code/doc index: file and chunk counts, per-language breakdown, last index time, and which indexed files are stale (changed) or missing (deleted on disk) since indexing.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project slug (optional)"
        },
        "check_staleness": {
          "type": "boolean",
          "description": "Re-hash indexed files against disk (default: true, capped at 200 files)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_list",
    "description": "List memory items without a vector search — for browsing checkpoints, reviewing what's stored, or finding items to deprecate.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "memory_layer": {
          "type": "string",
          "enum": [
            "L1",
            "L2",
            "L3",
            "L4"
          ],
          "description": "Filter by memory layer (optional)"
        },
        "checkpoints_only": {
          "type": "boolean",
          "description": "Return only checkpoint items (default: false)"
        },
        "include_deprecated": {
          "type": "boolean",
          "description": "Include deprecated items (default: false)"
        },
        "project": {
          "type": "string",
          "description": "Filter by project name (optional)"
        },
        "limit": {
          "type": "integer",
          "description": "Max items to return (default: 50)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_task_add",
    "description": "Add a new task to remember for later. Use this when the user mentions something they want to do later, a feature to build, or any pending work item.",
    "parameters": {
      "type": "object",
      "properties": {
        "title": {
          "type": "string",
          "description": "Short task title"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "description": {
          "type": "string",
          "description": "Detailed description (optional)"
        },
        "priority": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "description": "Priority (default: medium)"
        },
        "project": {
          "type": "string",
          "description": "Project name for filtering (optional)"
        }
      },
      "required": [
        "title",
        "user_id"
      ]
    }
  },
  {
    "name": "context_task_list",
    "description": "List pending or all tasks. Call this at the start of a session to see what work is waiting.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "in_progress",
            "done",
            "cancelled"
          ],
          "description": "Filter by status (default: pending)"
        },
        "priority": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "description": "Filter by priority (optional)"
        },
        "project": {
          "type": "string",
          "description": "Filter by project (optional)"
        },
        "limit": {
          "type": "integer",
          "description": "Max tasks to return (default: 50)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_task_update",
    "description": "Update a task — change its status, priority, title, or description.",
    "parameters": {
      "type": "object",
      "properties": {
        "task_id": {
          "type": "string",
          "description": "Task ID to update"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "status": {
          "type": "string",
          "enum": [
            "pending",
            "in_progress",
            "done",
            "cancelled"
          ],
          "description": "New status"
        },
        "priority": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "description": "New priority"
        },
        "title": {
          "type": "string",
          "description": "Updated title"
        },
        "description": {
          "type": "string",
          "description": "Updated description"
        }
      },
      "required": [
        "task_id",
        "user_id"
      ]
    }
  },
  {
    "name": "context_set_project",
    "description": "Set the active project for the current user. All subsequent memory operations (store, retrieve, list, ingest, checkpoint) will be scoped to a dedicated Qdrant collection for this project (e.g. 'synatyx' → collection 'ctx_synatyx'). The selection is persisted in Redis and survives server restarts. If unsure of the project name, call context_get_project first — it will suggest the workspace folder name.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project name to activate (e.g. 'synatyx', 'taty-v2'). Will be slugified automatically."
        }
      },
      "required": [
        "user_id",
        "project"
      ]
    }
  },
  {
    "name": "context_get_project",
    "description": "Return the currently active project for the user. If no project has been set, returns a suggestion based on the current workspace folder name — call context_set_project with the suggested name to confirm.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_skill_store",
    "description": "Save a skill definition. Writes full content to PostgreSQL and embeds only the description into Qdrant L3 for RAG matching.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Skill name (e.g. 'nodejs-developer')"
        },
        "description": {
          "type": "string",
          "description": "One-line description used for RAG matching"
        },
        "content": {
          "type": "string",
          "description": "Full skill content (system prompt + instructions)"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project scope (optional, null = global)"
        },
        "frontmatter": {
          "type": "object",
          "description": "Parsed YAML frontmatter fields (optional)"
        }
      },
      "required": [
        "name",
        "description",
        "content",
        "user_id"
      ]
    }
  },
  {
    "name": "context_skill_find",
    "description": "RAG search for the best matching skill(s) for a given task. Embeds the query, searches Qdrant L3 filtered by type='skill', then fetches full content from PostgreSQL.",
    "parameters": {
      "type": "object",
      "properties": {
        "query": {
          "type": "string",
          "description": "Task description to match against stored skills"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Limit search to a specific project (optional)"
        },
        "top_k": {
          "type": "integer",
          "description": "Max number of skills to return (default: 3)"
        }
      },
      "required": [
        "query",
        "user_id"
      ]
    }
  },
  {
    "name": "context_skill_get",
    "description": "Fetch a skill by exact name or slug from PostgreSQL.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Skill name or slug to look up"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project scope filter (optional)"
        }
      },
      "required": [
        "name",
        "user_id"
      ]
    }
  },
  {
    "name": "context_skill_list",
    "description": "List all stored skills for the user, optionally scoped to a project.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Filter by project (optional)"
        },
        "limit": {
          "type": "integer",
          "description": "Max skills to return (default: 50)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_skill_delete",
    "description": "Remove a skill from PostgreSQL and deprecate its Qdrant embedding.",
    "parameters": {
      "type": "object",
      "properties": {
        "name": {
          "type": "string",
          "description": "Skill name or slug to delete"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        }
      },
      "required": [
        "name",
        "user_id"
      ]
    }
  },
  {
    "name": "context_gc_stats",
    "description": "Return garbage collection statistics for the active project — how many items are expiring soon, already deprecated, pending hard delete, or protected.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project slug to operate in — overrides the active-project pointer for this call. Pass it in multi-session setups so concurrent sessions in different projects don't route into each other's collections (optional)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_consolidate",
    "description": "Manually trigger sleep-style memory consolidation for the active project: clusters of similar episodic (L2) memories (cosine >= threshold, cluster size >= minimum) are merged into a single L3 fact; the originals are deprecated (never deleted) and linked to the merged item with supersedes edges. Attempt records, pinned items, and existing consolidations are never merged. The GC daemon also runs this automatically in the background when enabled.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project slug to operate in — overrides the active-project pointer for this call. Pass it in multi-session setups so concurrent sessions in different projects don't route into each other's collections (optional)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_usage",
    "description": "Report token spend metered per tool call: outbound context tokens (what tool responses inject into the agent's context), inbound argument tokens, and embedding-API tokens with estimated USD cost. Returns totals plus a breakdown grouped by tool, project, or day.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Only count calls attributed to this project slug (optional)"
        },
        "days": {
          "type": "integer",
          "description": "Time window in days (default: 30)"
        },
        "group_by": {
          "type": "string",
          "enum": [
            "tool",
            "project",
            "day"
          ],
          "description": "Breakdown dimension (default: tool)"
        }
      },
      "required": [
        "user_id"
      ]
    }
  },
  {
    "name": "context_deprecate_source",
    "description": "Deprecate every live item ingested under one source id, and report how many. This is how a document is retired or replaced: a caller never learns the item ids its document became, so addressing them one by one is not possible. Items are deprecated, not deleted, so history and supersedes chains stay readable. A source that is already gone returns 0 — a successful no-op, not an error.",
    "parameters": {
      "type": "object",
      "properties": {
        "source_id": {
          "type": "string",
          "description": "The source id whose items to retire"
        },
        "user_id": {
          "type": "string",
          "description": "User identifier"
        },
        "project": {
          "type": "string",
          "description": "Project slug to operate in — overrides the active-project pointer for this call (optional)"
        },
        "reason": {
          "type": "string",
          "description": "Why the source is being retired (optional)"
        }
      },
      "required": [
        "source_id",
        "user_id"
      ]
    }
  },
  {
    "name": "context_erase_user",
    "description": "Hard-delete every item belonging to a user id, across every collection — project collections, the shared L4 collection, and per-project code indexes. For erasure requests, where deprecation is not an answer. This cannot be undone. Covers the vector store; rows outside it are not touched.",
    "parameters": {
      "type": "object",
      "properties": {
        "target_user_id": {
          "type": "string",
          "description": "The user id to erase. Named distinctly from user_id so erasing someone is always deliberate, never a defaulted argument."
        },
        "user_id": {
          "type": "string",
          "description": "User identifier of the caller"
        }
      },
      "required": [
        "target_user_id",
        "user_id"
      ]
    }
  }
]
